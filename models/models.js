const logger = require('../config/configUtils');
// const sqlite3 = require('sqlite3').verbose();

// exports.module = (tableName) => {
//     const db = new sqlite3.Database('student.db', (err) => {
//         if(err){
//             logger.error(`${tableName} database start error: ${err}`)
//         }
//         logger.info(`${tableName} database start`)
//     });
// }